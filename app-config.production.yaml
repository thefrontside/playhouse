app:
  # Should be the same as backend.baseUrl when using the `app-backend` plugin
  baseUrl: https://frontside-backstage-production.herokuapp.com

backend:
  baseUrl: https://frontside-backstage-production.herokuapp.com
  listen:
    port: ${PORT}
