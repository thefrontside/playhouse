import { scaffolderFrontendWorkflowPlugin } from './plugin';

describe('scaffolder-frontend-workflow', () => {
  it('should export plugin', () => {
    expect(scaffolderFrontendWorkflowPlugin).toBeDefined();
  });
});
